apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ingest-service.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "ingest-service.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "ingest-service.name" . }}
    spec:
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.containerPort }}
